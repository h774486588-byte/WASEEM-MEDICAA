name: Runner Smoke Test

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: |
          echo "GitHub Actions runner is available"
          uname -a
          java -version || true
